import { View } from 'react-native';

export default function CustomSplashScreen() {
  return (
    <View style={{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: '#ffffff' // Match your app theme
    }}>
      {/* TODO: Add custom splash screen content here. */}
    </View>
  );
}